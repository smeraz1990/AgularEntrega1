import { PipeFechaNacPipe } from './pipe-fecha-nac.pipe';

describe('PipeFechaNacPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFechaNacPipe();
    expect(pipe).toBeTruthy();
  });
});
